; Applied at request startup - unlike ini_set() inside the script, this also
; suppresses startup warnings (e.g. "POST Content-Length exceeds the limit"),
; which would otherwise be printed before the JSON body and corrupt it.
display_errors = Off
log_errors = On
